

## Two Quick Fixes in `src/components/CompetitionDetail.tsx`

1. **Judges section**: When judges array is empty or has no valid names, show a styled "TBD" placeholder instead of an empty grid. Applies to all competitions.

2. **Remove Crown icons** flanking "National Champion" text in the RAS 1st place card — keep just the text.

### Files Changed
- `src/components/CompetitionDetail.tsx`

